server {
    listen 3000;

    root /usr/share/nginx/html;
    index index.html;

    # Enable DNS resolver for dynamic service discovery
    resolver 127.0.0.11 valid=10s;
    set $auth_service "http://auth-service:3001";
    set $api_service "http://monolith:8080";

    # Proxy API requests to auth-service
    location /auth/ {
        proxy_pass $auth_service;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Proxy API requests to monolith backend
    location /api/ {
        proxy_pass $api_service;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Static files
    location /assets/ {
        try_files $uri =404;
    }

    # Support React Router / SPA - catch all remaining requests
    location / {
        try_files $uri /index.html;
    }
}
