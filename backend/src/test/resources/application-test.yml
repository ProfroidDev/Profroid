minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin123
  bucket-private: profroid-private

jwt:
  secret: dev-secret

stripe:
  secret-key: sk_test_dummy
  webhook-secret: whsec_dummy

spring:
  sql:
    init:
      mode: never
  jpa:
    hibernate:
      ddl-auto: create-drop
